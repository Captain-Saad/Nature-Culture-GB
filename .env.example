# Copy this file to .env.local (gitignored) and fill in real values.

# Base URL of the backend API (see /backend). Used server-side only today
# (Server Components, Route Handlers) for the admin auth flow; public
# pages still read from /lib/mock-data until their own wiring pass.
NEXT_PUBLIC_API_URL=http://localhost:4100
